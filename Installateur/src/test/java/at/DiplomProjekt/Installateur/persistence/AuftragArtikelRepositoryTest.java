package at.DiplomProjekt.Installateur.persistence;
/*
import at.DiplomProjekt.Installateur.TestFixtures;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;

import static org.assertj.core.api.Assertions.assertThat;
import static org.assertj.core.api.Assumptions.assumeThat;

@DataJpaTest
public class AuftragArtikelRepositoryTest {

    @Autowired
    private AuftragArtikelRepository repository;


    @Test
    void ensureSaveAndReadWorks() {

        var auftragArtikel = TestFixtures.auftragArtikel();


        var saved = repository.saveAndFlush(auftragArtikel);


        assertThat(saved).isSameAs(auftragArtikel);
        assertThat(saved.getId()).isNotNull();
        assumeThat(repository).isNotNull();


    }
}


 */