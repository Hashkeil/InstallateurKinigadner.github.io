package at.DiplomProjekt.Installateur.persistence;
/*
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;
import org.springframework.beans.factory.annotation.Autowired;
import static at.DiplomProjekt.Installateur.TestFixtures.mo;
import static org.assertj.core.api.Assertions.assertThat;
import static org.assertj.core.api.Assumptions.assumeThat;


@DataJpaTest
public class KundenRepositoryTest {

    private @Autowired KundenRepository kundenRepository;

    @Test
    void ensureSaveAndReadWorks() {


        var kunde = mo();



        var savedKunde = kundenRepository.saveAndFlush(kunde);

        assumeThat(kundenRepository).isNotNull();
        assumeThat(kundenRepository.findAll()).isNotEmpty();

        assertThat(savedKunde.getAddress().getCountry().getId()).isNotNull();
        assertThat(savedKunde).isSameAs(kunde);
        assertThat(savedKunde.getId()).isNotNull();
    }
}




 */