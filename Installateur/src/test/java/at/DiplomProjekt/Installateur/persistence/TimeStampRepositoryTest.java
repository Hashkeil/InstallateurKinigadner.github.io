package at.DiplomProjekt.Installateur.persistence;
/*
import at.DiplomProjekt.Installateur.TestFixtures;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;


import static org.assertj.core.api.Assertions.assertThat;
import static org.assertj.core.api.Assumptions.assumeThat;
import static org.junit.jupiter.api.Assertions.*;
@DataJpaTest
class TimeStampRepositoryTest {

    @Autowired
    private TimeStampRepository timeStampRepository;

    @Test
    void  ensureSaveandReReadWorks(){


        var timeStamp = TestFixtures.timeStamp();



        var savedTimeStamp = timeStampRepository.saveAndFlush(timeStamp);
        assertNotNull(savedTimeStamp);
        assumeThat(timeStampRepository.findAll()).isNotEmpty();
        assertNotNull(savedTimeStamp.getId());
        assertThat(savedTimeStamp).isNotNull().isSameAs(timeStamp);
        assumeThat(savedTimeStamp.getId()).isNotNull();








    }

}



 */