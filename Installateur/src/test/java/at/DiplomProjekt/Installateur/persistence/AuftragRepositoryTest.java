package at.DiplomProjekt.Installateur.persistence;
/*
import at.DiplomProjekt.Installateur.TestFixtures;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;

import static org.assertj.core.api.Assertions.assertThat;
import static org.assertj.core.api.Assumptions.assumeThat;

@DataJpaTest
class AuftragRepositoryTest {


    @Autowired
    private AuftragRepository auftragRepository;


    @Test
    void ensureSaveAndReadWorks() {


        var mitarbeiter = TestFixtures.mitarbeiter("Alex","Huber");
        var auftrag = TestFixtures.auftrag(mitarbeiter);

        var saved = auftragRepository.saveAndFlush(auftrag);

        assertThat(saved).isSameAs(auftrag);
        assertThat(saved.getId()).isNotNull();
        assumeThat(auftragRepository).isNotNull();


    }

}



 */