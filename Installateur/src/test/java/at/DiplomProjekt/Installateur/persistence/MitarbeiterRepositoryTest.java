package at.DiplomProjekt.Installateur.persistence;
/*
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;

import static at.DiplomProjekt.Installateur.TestFixtures.*;
import static org.assertj.core.api.Assertions.assertThat;
import static org.assertj.core.api.Assumptions.assumeThat;

@DataJpaTest
public class MitarbeiterRepositoryTest{


    private @Autowired MitarbeiterRepository mitarbeiterRepository;

    @Test
    void ensureSaveAndReadWorks(){



        var mitarbeiter = am();

        var savedMitarbeiter = mitarbeiterRepository.saveAndFlush(mitarbeiter);

        assumeThat(mitarbeiterRepository).isNotNull();
        assumeThat(mitarbeiterRepository.findAll()).isNotEmpty();

        assertThat(savedMitarbeiter).isSameAs(mitarbeiter);
        assertThat(savedMitarbeiter.getId()).isNotNull();
    }


}
 */